export interface MemeCoin {
  month: string;
  controversial: string;
  highMindshare: string;
  highlights: string;
  image: string;
}

export enum Controls {
  forward = 'forward',
  backward = 'backward',
  left = 'left',
  right = 'right',
}
